version: "3.9"
services:
  nginx:
    build: ./nginx
    ports:
      - "8080:80"
    volumes:
      - static_storage:/usr/share/nginx/html
    container_name: nginx
  api:
    build: ./app
    volumes:
      - static_storage:/app_data
    container_name: api
volumes:
  static_storage:
